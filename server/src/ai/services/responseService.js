export const formatResponse = (rawResponse) => {
  // In a more complex app, we might parse specific structures or sanitize here.
  // For now, we return the raw markdown string safely.
  return rawResponse;
};
